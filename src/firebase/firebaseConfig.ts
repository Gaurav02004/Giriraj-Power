// Client-side local architecture (Firebase backend decoupled per configuration)
export const app = null;
export const auth = null;
export const db = null;
export const functions = null;

export default app;
